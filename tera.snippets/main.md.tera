## How to run

* `docker.io` must be installed and running.
* user running must be in group 'docker'.

## Number of examples

Currently there are {{ git_count_files(pattern="exercises/**/Dockerfile") + git_count_files(pattern="examples/**/Dockerfile") }} examples in this repo.
